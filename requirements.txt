# 核心依赖
requests>=2.20.0       # 用于同步 HTTP 请求 (可选，如果完全迁移到 async)
aiohttp>=3.8.0        # 用于异步 HTTP 请求
beautifulsoup4>=4.9.0 # 用于解析 HTML
aiofiles>=0.8.0       # 用于异步文件 I/O

# 注意：该项目还需要 Node.js 和 'esprima' npm 包。
# 1. 从 https://nodejs.org/ 安装 Node.js (LTS 版本推荐)
# 2. 在项目根目录下运行 `npm install esprima` 来安装 Esprima 解析器。
